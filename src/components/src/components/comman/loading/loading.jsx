import preloader from "../../friends/photoFriends/loading-87.gif"
import './loading.css'

let Loading = () => {
    return   (<img className ="loading" src={preloader} alt = "loading"/>  )

    
}
 export default Loading;